# The receiver reads only where it runs from the environment. Everything the
# device or HR can be wrong about is a database row (SPEC.md §9).
POSTGRES_PASSWORD=hr

# Half of the device's Cloud Server Setting. The device is pointed at 8081, so
# changing this means changing the setting on the device (SPEC.md §10).
RECEIVER_PORT=8081

# The HR interface's port. Tailscale reaches this one and only this one; the
# guard reaches it over factory Wi-Fi by the server's LAN address (SPEC.md §14).
HR_PORT=8090
DATABASE_URL=postgresql+psycopg://hr:hr@127.0.0.1:5432/hr_attendance
